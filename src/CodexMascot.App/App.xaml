<Application x:Class="CodexMascot.App.App"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             StartupUri="MainWindow.xaml" ShutdownMode="OnMainWindowClose">
    <Application.Resources>
        <SolidColorBrush x:Key="PanelBrush" Color="#F7161E2D" />
        <SolidColorBrush x:Key="CardBrush" Color="#FF202B3C" />
        <SolidColorBrush x:Key="TextBrush" Color="#FFF3F6FA" />
        <SolidColorBrush x:Key="MutedTextBrush" Color="#FFADB9C9" />
        <SolidColorBrush x:Key="AccentBrush" Color="#FF70D6FF" />
        <Style TargetType="Button">
            <Setter Property="Margin" Value="4" />
            <Setter Property="Padding" Value="10,6" />
            <Setter Property="Foreground" Value="{StaticResource TextBrush}" />
            <Setter Property="Background" Value="#FF314057" />
            <Setter Property="BorderBrush" Value="#FF53647D" />
        </Style>
        <Style TargetType="TextBox">
            <Setter Property="Margin" Value="4" />
            <Setter Property="Padding" Value="8,6" />
            <Setter Property="Foreground" Value="#FF17202E" />
        </Style>
    </Application.Resources>
</Application>
