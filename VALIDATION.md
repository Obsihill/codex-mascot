# 검증 기록

## Mascot Studio preview — 2026-09-27

- Windows x64, .NET SDK 9.0.306 / .NET 8 WPF, Release 빌드 경고 0·오류 0.
- Core 88개 + App 320개 = 408개 assertion 통과(로컬 MP4 fixture 포함).
- 설치됨/선택됨 카드의 +/−·더블 클릭·반투명 드래그, 중복 항목별 설정, 동시 팝업, 트레이 복원, 삭제 및 실행 취소/다시 실행을 검증함.
- 숫자 드래그/직접 입력·혼합값·취소·한 번의 실행 취소, 설치 정렬/선호 시간, 좌표 프리뷰, 이미지 재생시간의 실제 타이머·완료/승인 유지 정책을 검증함.
- 폴더 라이브러리 저장/백업/마이그레이션, 상대 경로와 MP4 바이트 보존, 등록 창 폴더 불러오기·취소·등록·복원, 손상/누락/외부 경로/Windows 경로 별칭 차단을 검증함.
- NAudio 음량 증폭의 샘플 수치·클리핑, 영상 재생/종료·무음·타이머를 검증함. 실제 스피커 청취 품질은 별도 수동 확인이 필요함.
- 배포 대상은 win-x64 self-contained 폴더/ZIP이며 개인 config/library, .local, 테스트 미디어, 인증 정보는 포함하지 않음. 공식 버전 태그·릴리스는 별도로 생성하지 않음.
- WPF 테스트 창을 렌더링하여 일반/작은 창 크기, 등록·이미지 재생시간·좌표·공통 설정의 배치를 확인함.

아래 0.2.1 기록은 당시 UI에 대한 기록입니다. Studio의 테스트/등록/위치 기능은 메인 화면으로 이동했으며 이전 설정의 테스트 버튼은 제거되었습니다.

배포 전 남은 수동 검증: 실제 사용자 계정의 신뢰된 Hooks 종단 경로, 모든 Windows/DPI/다중 모니터 조합, 재부팅 후 시작, 실제 음성 출력. 코드 서명을 추가하지 않은 preview이므로 Windows에서 게시자 경고가 표시될 수 있습니다.

## 0.2.1 추가 기능 검증

- Release 빌드: 경고 0, 오류 0.
- Core 52개, App 42개 assertion 통과.
- 완료 팝업의 시간 제한 해제, 옵션 변경 시 타이머 취소/재설정, 클릭 통과 예외, 클릭/× 닫기 분리, 다른 상태의 자동 닫힘을 실제 WPF 창과 메시지 루프로 확인함.
- 같은 작업의 다음 turn 시작 뒤에도 미확인 완료 유지, 승인/실패 우선순위, 작업별/전체 확인, 초기 이력 제외를 검증함.
- 새 옵션의 기존 JSON 기본값, 저장/재로드, 테마 내보내기/가져오기 확인.
- 최소화 복원 후 전환, 최대화 크기 유지, 창 없음, 전환 거부 시 작업 표시줄 알림은 주입한 Windows 호출 대역으로 검증함.
- 실제 실행 중인 Store Codex (`OpenAI.Codex_…/app/ChatGPT.exe`)의 창 탐색 성공. 일반 ChatGPT·Codex CLI·Mascot를 대상에서 제외함.
- 실제 사용자 클릭 뒤 다른 프로세스의 포커스 전환은 설정의 **Codex 창 열기 테스트**로 확인할 수 있음. Windows 포커스 정책에 따른 전환 성공은 모든 환경에서 보장하지 않음. 구현은 [SetForegroundWindow](https://learn.microsoft.com/en-us/windows/win32/api/winuser/nf-winuser-setforegroundwindow)와 [ShowWindowAsync](https://learn.microsoft.com/en-us/windows/win32/api/winuser/nf-winuser-showwindowasync)를 사용함.

2026-09-20, Windows x64 / .NET 8 WPF.

- 안정 .NET SDK 9.0.306 Release 빌드: 경고 0, 오류 0.
- Core: 43개 assertion 통과. 우선순위, 중복/지연 이벤트, 확인과 승인 분리, 다중 요청, CLI 이벤트, 손상 JSON, 부분 UTF-8 기록, 파일 잘림, 초기 이력 및 실시간 파일 추가 처리.
- App: 17개 assertion 통과. GIF 프레임별 시간, PNG 스프라이트, WebP, WAV 디코딩/미디어 열기(무음), 손상 이미지, Hook 병합/백업/해제/전달, 민감 원문 제외, 설정/테마 복원, 숫자형 RPC 승인, 서버 종료 복구.
- 실제 데스크톱 기록: 일반 작업 24개를 발견했고 현재 대화와 다른 프로젝트의 Running 상태를 읽음. 로컬 기록의 새 이벤트도 관찰함.
- 실제 UI: 감시 창의 작업 목록/현재 상태 및 설정 창의 미리보기·위치/모니터 선택·테스트 컨트롤을 확인함.
- 이후 사용자가 Escape로 Computer Use를 중지하여 추가 화면 조작은 하지 않음.

아직 검증하지 않은 범위:

- 사용자가 Codex에서 신뢰 처리한 Hook의 실제 발신부터 Windows 팝업까지 이어지는 종단 경로. 전달 스크립트와 수신기는 합성 이벤트로 테스트했으며 기본 기록 감시는 실제 데이터로 검증함.
- Windows 재부팅 후 자동 실행 및 모든 100/150/200% DPI·다중 모니터 조합.
- 실제 스피커에서의 청취 품질. WAV 미디어 디코딩과 재생 시작 이벤트는 검사함.
- 외부 데스크톱 작업의 모든 승인·오류. 공식 Hook/기록에 없는 상태는 추측하지 않음.

배포판은 자체 런타임을 포함합니다. 숨겨진 진단 명령으로 창 없이 기본 이미지 로드와 실제 기록 감시 초기화를 확인할 수 있습니다:

```powershell
CodexMascot.App.exe --diagnose report.json "C:\Users\사용자\.codex"
```

진단은 Codex 대화를 시작·재개하지 않으며 원문 대화 내용을 결과에 기록하지 않습니다.
